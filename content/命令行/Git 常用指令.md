---
title: Git 常用指令
tags:
  - Git
  - 命令行
---

## `git add .`

把当前目录下所有的修改文件放入暂存区，相当于放进购物车。

## `git commit -m "xxx"`

把暂存区内的东西保存记录，相当于把购物车里的东西打包，准备结账。`-m "xxx"` 是给它加一个备注。

## `git push`

把快照部署到 GitHub 上。

## `git status`

看看当前目录下修改了什么东西。
